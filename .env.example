# Required environment variables for MCP Gateway

# OAUTH2 Configuration
OAUTH2_ISSUER= # URL of the OAuth2 issuer
OAUTH2_JWKS_URL= # URL of the JWKS endpoint

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=mcp
DB_PASSWORD=secret
DB_NAME=mcp

# OPA Configuration
OPA_URL=http://localhost:8181

# Port Configuration
PORT=8080
